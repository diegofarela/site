/* Local / Docker default: dev tools allowed (still gated by localhost / ?dev=1).
   GitHub Pages deploy overwrites this file with DEV_TOOLS: false. */
window.__PORTFOLIO_ENV__ = {
  DEV_TOOLS: true
};
