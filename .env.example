# Copied into js/env.js at publish time (see scripts/inject-env.js).
# true  = allow viewport / scene-quality tools in local development
# false = hard-disable on the public GitHub Pages build (even ?dev=1)
PORTFOLIO_DEV_TOOLS=true
